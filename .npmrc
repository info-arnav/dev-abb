# Platform-specific package handling
auto-install-peers=true
legacy-peer-deps=false
fund=false
audit=false

# Disable optional dependencies to avoid platform issues
optional=false
ignore-optional=true
